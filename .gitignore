# Maven
target/

# IDE
.idea/
*.iml
*.iws
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store

# Logs
*.log

# Environment / secrets.
# Ignored by prefix, not by exact name: editors and backups produce .env~, .env.swp
# and .env.old, and each of those holds the same credentials. Only the template is
# allowed back in.
.env*
!.env.example

# Personal project context (not meant to be shared in the public repo)
PROJECT_CONTEXT.md

# Personal learning log (not meant to be shared in the public repo)
conhecimentos-adquiridos.md
