# Copy to `.env` and fill in the blank value. `.env` is gitignored and must never
# be committed — this template exists so the repository documents which variables
# are needed without ever carrying their values.

# --- Local PostgreSQL server (Docker Compose) ---
POSTGRES_DB=jobtracker
POSTGRES_USER=jobtracker
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
